source "https://rubygems.org"

# The github-pages gem pins Jekyll + plugins to exactly what GitHub
# Pages runs in production, so a local build matches the deployed site.
gem "github-pages", group: :jekyll_plugins

# Needed for `jekyll serve` on Ruby 3.0+ (webrick left the stdlib).
gem "webrick", "~> 1.8"
